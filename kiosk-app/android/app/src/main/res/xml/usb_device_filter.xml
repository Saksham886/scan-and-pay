<?xml version="1.0" encoding="utf-8"?>
<!-- VID:PID 1fc9:2016 ("USB Printer Port") - the 80mm receipt printer's
     controller, confirmed via the in-app USB diagnostic view to expose the
     standard USB printer class with a bulk-OUT endpoint. Decimal below
     because Android's device filter format wants decimal, not hex. -->
<resources>
    <usb-device vendor-id="8137" product-id="8214"/>
</resources>
